# Read about fixtures at http://api.rubyonrails.org/classes/Fixtures.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
assign_one: 
  user_id: <%= ActiveRecord::Fixtures.identify(:staff_one) %>
  need_id: <%= ActiveRecord::Fixtures.identify(:lights) %>
# column: value
#
assign_two:
  user_id: <%= ActiveRecord::Fixtures.identify(:staff_two) %>
  need_id: <%= ActiveRecord::Fixtures.identify(:air) %>

assign_three:
  user_id: <%= ActiveRecord::Fixtures.identify(:staff_two) %>
  need_id: <%= ActiveRecord::Fixtures.identify(:setup) %>
